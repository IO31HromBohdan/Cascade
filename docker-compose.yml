services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: cascade
      POSTGRES_PASSWORD: cascade
      POSTGRES_DB: cascade_db
    ports:
      - '5432:5432'
    volumes:
      - cascade_db_data:/var/lib/postgresql/data

volumes:
  cascade_db_data:
